# parse-env-file

## Building

Run `npx nx build parse-env-file` to build the library.

## Running unit tests

Run `npx nx test parse-env-file` to execute the unit tests via [Jest](https://jestjs.io).
