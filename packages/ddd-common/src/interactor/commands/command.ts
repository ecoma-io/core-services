/**
 * Marker interface for a command message.
 *
 * @remarks
 * Commands represent an intention to perform an action inside the application
 * and are handled by a `CommandHandler` implementation.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface, @typescript-eslint/no-empty-object-type
export interface ICommand {
  // marker interface for commands
}
