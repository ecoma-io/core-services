# Redis Commander - Redis management UI
# Base image from official Redis Commander
FROM rediscommander/redis-commander:latest

# Add any custom configurations here if needed in the future
# COPY custom-config.json /app/config.json
# RUN npm install additional-package

# Default command is inherited from base image
