# acm-projector-interactor

## Building

Run `npx nx build acm-projector-interactor` to build the library.

## Running unit tests

Run `npx nx test acm-projector-interactor` to execute the unit tests via [Jest](https://jestjs.io).
