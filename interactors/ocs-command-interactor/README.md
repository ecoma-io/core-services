# ocs-command-interactor

## Building

Run `npx nx build ocs-command-interactor` to build the library.

## Running unit tests

Run `npx nx test ocs-command-interactor` to execute the unit tests via [Jest](https://jestjs.io).
