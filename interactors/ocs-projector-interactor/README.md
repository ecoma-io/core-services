# ocs-projector-interactor

## Building

Run `npx nx build ocs-projector-interactor` to build the library.

## Running unit tests

Run `npx nx test ocs-projector-interactor` to execute the unit tests via [Jest](https://jestjs.io).
