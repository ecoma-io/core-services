# azm-projector-interactor

## Building

Run `npx nx build azm-projector-interactor` to build the library.

## Running unit tests

Run `npx nx test azm-projector-interactor` to execute the unit tests via [Jest](https://jestjs.io).
