# idm-command-interactor

## Building

Run `npx nx build idm-command-interactor` to build the library.

## Running unit tests

Run `npx nx test idm-command-interactor` to execute the unit tests via [Jest](https://jestjs.io).
