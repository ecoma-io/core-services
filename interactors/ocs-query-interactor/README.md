# ocs-query-interactor

## Building

Run `npx nx build ocs-query-interactor` to build the library.

## Running unit tests

Run `npx nx test ocs-query-interactor` to execute the unit tests via [Jest](https://jestjs.io).
