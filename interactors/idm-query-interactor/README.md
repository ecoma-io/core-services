# idm-query-interactor

## Building

Run `npx nx build idm-query-interactor` to build the library.

## Running unit tests

Run `npx nx test idm-query-interactor` to execute the unit tests via [Jest](https://jestjs.io).
