# rsm-query-interactor

## Building

Run `npx nx build rsm-query-interactor` to build the library.

## Running unit tests

Run `npx nx test rsm-query-interactor` to execute the unit tests via [Jest](https://jestjs.io).
