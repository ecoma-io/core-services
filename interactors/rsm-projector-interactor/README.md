# rsm-projector-interactor

## Building

Run `npx nx build rsm-projector-interactor` to build the library.

## Running unit tests

Run `npx nx test rsm-projector-interactor` to execute the unit tests via [Jest](https://jestjs.io).
