# azm-query-interactor

## Building

Run `npx nx build azm-query-interactor` to build the library.

## Running unit tests

Run `npx nx test azm-query-interactor` to execute the unit tests via [Jest](https://jestjs.io).
