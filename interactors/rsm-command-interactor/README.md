# rsm-command-interactor

## Building

Run `npx nx build rsm-command-interactor` to build the library.

## Running unit tests

Run `npx nx test rsm-command-interactor` to execute the unit tests via [Jest](https://jestjs.io).
