# acm-query-interactor

## Building

Run `npx nx build acm-query-interactor` to build the library.

## Running unit tests

Run `npx nx test acm-query-interactor` to execute the unit tests via [Jest](https://jestjs.io).
