# azm-command-interactor

## Building

Run `npx nx build azm-command-interactor` to build the library.

## Running unit tests

Run `npx nx test azm-command-interactor` to execute the unit tests via [Jest](https://jestjs.io).
