# idm-projector-interactor

## Building

Run `npx nx build idm-projector-interactor` to build the library.

## Running unit tests

Run `npx nx test idm-projector-interactor` to execute the unit tests via [Jest](https://jestjs.io).
