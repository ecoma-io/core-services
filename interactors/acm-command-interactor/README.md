# acm-command-interactor

## Building

Run `npx nx build acm-command-interactor` to build the library.

## Running unit tests

Run `npx nx test acm-command-interactor` to execute the unit tests via [Jest](https://jestjs.io).
