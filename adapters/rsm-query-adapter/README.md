# rsm-query-adapter

## Building

Run `npx nx build rsm-query-adapter` to build the library.

## Running unit tests

Run `npx nx test rsm-query-adapter` to execute the unit tests via [Jest](https://jestjs.io).
