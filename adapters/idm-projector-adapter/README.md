# idm-projector-adapter

## Building

Run `npx nx build idm-projector-adapter` to build the library.

## Running unit tests

Run `npx nx test idm-projector-adapter` to execute the unit tests via [Jest](https://jestjs.io).
