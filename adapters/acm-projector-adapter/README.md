# acm-projector-adapter

## Building

Run `npx nx build acm-projector-adapter` to build the library.

## Running unit tests

Run `npx nx test acm-projector-adapter` to execute the unit tests via [Jest](https://jestjs.io).
