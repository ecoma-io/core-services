# azm-query-adapter

## Building

Run `npx nx build azm-query-adapter` to build the library.

## Running unit tests

Run `npx nx test azm-query-adapter` to execute the unit tests via [Jest](https://jestjs.io).
