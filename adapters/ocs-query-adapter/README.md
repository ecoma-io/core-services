# ocs-query-adapter

## Building

Run `npx nx build ocs-query-adapter` to build the library.

## Running unit tests

Run `npx nx test ocs-query-adapter` to execute the unit tests via [Jest](https://jestjs.io).
