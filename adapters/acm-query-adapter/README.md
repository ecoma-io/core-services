# acm-query-adapter

## Building

Run `npx nx build acm-query-adapter` to build the library.

## Running unit tests

Run `npx nx test acm-query-adapter` to execute the unit tests via [Jest](https://jestjs.io).
