# idm-query-adapter

## Building

Run `npx nx build idm-query-adapter` to build the library.

## Running unit tests

Run `npx nx test idm-query-adapter` to execute the unit tests via [Jest](https://jestjs.io).
