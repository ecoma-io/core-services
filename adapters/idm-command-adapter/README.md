# idm-command-adapter

## Building

Run `npx nx build idm-command-adapter` to build the library.

## Running unit tests

Run `npx nx test idm-command-adapter` to execute the unit tests via [Jest](https://jestjs.io).
