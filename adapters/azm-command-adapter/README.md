# azm-command-adapter

## Building

Run `npx nx build azm-command-adapter` to build the library.

## Running unit tests

Run `npx nx test azm-command-adapter` to execute the unit tests via [Jest](https://jestjs.io).
