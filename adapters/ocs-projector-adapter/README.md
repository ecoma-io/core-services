# ocs-projector-adapter

## Building

Run `npx nx build ocs-projector-adapter` to build the library.

## Running unit tests

Run `npx nx test ocs-projector-adapter` to execute the unit tests via [Jest](https://jestjs.io).
