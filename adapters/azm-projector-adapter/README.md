# azm-projector-adapter

## Building

Run `npx nx build azm-projector-adapter` to build the library.

## Running unit tests

Run `npx nx test azm-projector-adapter` to execute the unit tests via [Jest](https://jestjs.io).
