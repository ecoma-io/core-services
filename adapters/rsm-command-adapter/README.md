# rsm-command-adapter

## Building

Run `npx nx build rsm-command-adapter` to build the library.

## Running unit tests

Run `npx nx test rsm-command-adapter` to execute the unit tests via [Jest](https://jestjs.io).
