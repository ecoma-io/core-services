# ocs-command-adapter

## Building

Run `npx nx build ocs-command-adapter` to build the library.

## Running unit tests

Run `npx nx test ocs-command-adapter` to execute the unit tests via [Jest](https://jestjs.io).
