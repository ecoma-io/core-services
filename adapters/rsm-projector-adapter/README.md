# rsm-projector-adapter

## Building

Run `npx nx build rsm-projector-adapter` to build the library.

## Running unit tests

Run `npx nx test rsm-projector-adapter` to execute the unit tests via [Jest](https://jestjs.io).
