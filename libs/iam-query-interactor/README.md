# iam-query-interactor

## Building

Run `npx nx build iam-query-interactor` to build the library.

## Running unit tests

Run `npx nx test iam-query-interactor` to execute the unit tests via [Jest](https://jestjs.io).
