# iam-command-interactor

## Building

Run `npx nx build iam-command-interactor` to build the library.

## Running unit tests

Run `npx nx test iam-command-interactor` to execute the unit tests via [Jest](https://jestjs.io).
